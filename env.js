// Form submissions are proxied through Vercel serverless: /api/contact
window.GMAIL_ENDPOINT = '/api/contact';
// Shared secret to pass to Apps Script (must match SECRET in the script)
window.FORM_SECRET = 'CHANGE_ME_123';
